package net.pushfree.android

import org.junit.Assert.assertEquals
import org.junit.Test

/** Smoke test proving the unit-test variant compiles and runs on JVM. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
