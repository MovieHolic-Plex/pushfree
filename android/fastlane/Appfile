# frozen_string_literal: true
#
# fastlane Appfile - Pushfree (todo 49).
#
# Placeholders ONLY. No real credentials live in this file (or anywhere in this
# repo). Supply the values through environment variables at upload time; see
# fastlane/Fastfile for the documented env contract.

# Play Store package name = the `play` flavor's applicationId.
# (Override at runtime with the SUPPLY_PACKAGE_NAME env var.)
package_name(ENV.fetch('SUPPLY_PACKAGE_NAME', 'net.pushfree.android.play'))

# Google Play service account JSON key. Uncomment ONE of the lines below and
# supply the value via env at upload time - do NOT hard-code a path or contents
# here.
#
# json_key_file(ENV['SUPPLY_JSON_KEY_PATH']) if ENV.key?('SUPPLY_JSON_KEY_PATH')
# json_key_data(ENV['SUPPLY_JSON_KEY'])      if ENV.key?('SUPPLY_JSON_KEY')

# Apple App ID / issuer is intentionally absent: there is no iOS client
# (scope: Must NOT have - iOS).
